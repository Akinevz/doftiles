#!/bin/bash

export DISPLAY=$(grep -m 1 nameserver /etc/resolv.conf | awk '{print $2}'):0
export LIBGL_ALWAYS_INDIRECT=1
export PULSE_SERVER=$(grep -m 1 nameserver /etc/resolv.conf | awk '{print $2}')

